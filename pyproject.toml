[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resume-job-matcher"
version = "0.1.0"
description = "Explainable resume <-> job fit scorer (IE 7500, Group 6)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
