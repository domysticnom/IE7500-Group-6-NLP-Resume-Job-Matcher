# Install the CUDA build of PyTorch FIRST, separately, from https://pytorch.org
#   e.g. pip install torch --index-url https://download.pytorch.org/whl/cu124
#   verify: python -c "import torch; print(torch.cuda.is_available())"  -> True
# Then: pip install -r requirements.txt
# Then: pip install -e .

scikit-learn==1.8.0
transformers==5.9.0
datasets==4.8.5
accelerate==1.13.0
pandas==2.2.3
kagglehub==0.3.13
streamlit==1.57.0
matplotlib
seaborn
tqdm
pypdf
